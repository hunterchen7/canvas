name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          # The release type determines how the version is bumped
          release-type: node
          # Optional: customize the package name
          package-name: '@hunterchen/canvas'
